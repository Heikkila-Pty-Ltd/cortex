## Instructions
1. Read the acceptance criteria carefully
2. Implement in the files listed (create if needed)
3. Run tests if they exist
4. Commit with message: feat({{ .Bead.ID }}): {{ .ShortTitle }}
5. When done, run: bd close {{ .Bead.ID }}
6. Push: {{if .UseBranches}}git push (PR creation will be handled automatically){{else}}git push{{end}}
